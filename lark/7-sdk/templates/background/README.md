# Background Application Template

This template demonstrates an application that runs without a window.

## Architecture
Applications in LDE are not required to create windows. They can run background processes, register for intents, or periodically show notifications.

## Concepts demonstrated
- Running without `context.window.create()`
- Using intervals for background tasks
- Triggering notifications using capabilities
