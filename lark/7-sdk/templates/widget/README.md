# Widget Extension Template

This template demonstrates how to create a Widget extension.

## Architecture
This package does not have an application entry point. Instead, it defines an `extensions` array in the manifest and points to a widget component class.

## Concepts demonstrated
- Type `extension` in `manifest.json`
- Defining a `Widget` extension
- The widget component interface (`mount`, `unmount`)
