# Minimal Application Template

This is the most basic structure for a Lark Desktop Environment (LDE) application.

## Structure
- `manifest.json`: Defines the application's identity and capabilities.
- `App.js`: The entry point that extends `BaseApplication`.

## Concepts demonstrated
- Basic manifest definition
- Launching a simple window
- Directly manipulating window content DOM
